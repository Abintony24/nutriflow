apiVersion: apps/v1
kind: Deployment
metadata:
  name: nutriflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nutriflow
  template:
    metadata:
      labels:
        app: nutriflow
    spec:
      containers:
        - name: nutriflow
          image: 504483085764.dkr.ecr.us-east-1.amazonaws.com/nutriflow:latest
          ports:
            - containerPort: 8000
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: nutriflow-secret
                  key: MONGO_URI
